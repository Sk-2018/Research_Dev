
# Payload Diff Viewer (Windows, Tkinter)

Updated build (v1.0.1) — fixes DeepDiff SetOrdered error by switching to text view and improves table population.

## Install
```bash
pip install "deepdiff>=6.7.1" "pandas>=2.0.0" "openpyxl>=3.1.2" "matplotlib>=3.8.0" "orjson>=3.9.15"
```

## Run
```bash
python run_app.py
```
